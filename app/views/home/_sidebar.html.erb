
        <div id="sidebar" class="col-md-4">
          <div class="row">
            <section class="col-md-12 wow fadeIn">
              <div class="about">

                <h3>Smart Gioiosa</h3>
                <img src="/assets/avatar_tatta.png" class="avatar-full-width img-responsive" />


                <p><%= link_to 'Chi Siamo', chi_siamo_path, class:"btn btn-ada-dark"%></p>

              </div>
            </section>
            <%if controller.controller_name=="posts"%>
              <%if @tags.exists?%>
              <section class="col-md-12 wow fadeIn">
                <div class="tags">

                  <h3>Tags</h3>
                  <% @tags.each do |tag| %>
                  <%= link_to '#'+tag.name, tag_path(tag), class:"btn btn-ada-dark btn-tag" %>
                  <%end%>
                </div>
              </section>
              <%end%>
            <%end%>
            <%if controller.controller_name=="home"%>
              <section class="col-md-12 wow fadeIn">
                <div class="popular-posts">

                  <h3>Ultmi Progetti</h3>

                  <%@sidebar_project.each do |project|%>
                    <%unless project.image_preview.blank?%>
                      <%= link_to image_tag(project.image_preview.url(:thumb2), class:"img-responsive" ), project_path(project)%>
                    <%end%>
                    <h4> <%= link_to project.title,project_path(project) %></h4>
                  <%end%>


                </div>
              </section>
            <%end%>

          </div>
        </div>

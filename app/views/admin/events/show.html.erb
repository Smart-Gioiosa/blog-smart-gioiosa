<div class="row">
  <h1><%=@event.title%></h1>
  <p><%= @event.body%></p>
</div>

<div class ="row">
  <div class="page-header">
    <ul class="actions">
    <li><%= link_to fa_icon("pencil") + " Edit Post", edit_admin_event_path(@event),remote: true, class: "btn btn-primary" %></li>
    </ul>
  </div>
</div>

<%= link_to 'Back', admin_events_path, class: "btn btn-primary" %>
